package handlers

import "github.com/gofiber/fiber/v2"

// 获取服务器分组列表
func GroupList(c *fiber.Ctx) error { return nil }

// 更新服务器分组信息
func UpdateGroup(c *fiber.Ctx) error { return nil }

// 新建服务器分组
func CreateGroup(c *fiber.Ctx) error { return nil }

// 删除服务器分组
func DeleteGroup(c *fiber.Ctx) error { return nil }
